# Интеграция с Яндекс Картами

Мини‑CRM, которая позволяет авторизоваться, сохранить ссылку на карточку Яндекс.Карт и просматривать отзывы.  
Сейчас отзывы и рейтинг отдаются из заглушки (см. `ReviewsService`) — это позволяет показать UI без реального API‑ключа.  
После получения ключа нужно всего лишь раскомментировать вызов провайдера.

## 🛠 Технологический стек

- **Backend**: Laravel 12
- **Frontend**: Vue.js 3 + Tailwind CSS
- **База данных**: SQLite (можно переключить на MySQL/PostgreSQL)

## 📦 Возможности

- Авторизация по email/паролю.
- Страница «Настройка» с валидацией ссылки на Яндекс.Карты.
- Автоматическое извлечение ID организации из ссылки.
- Страница «Отзывы» со списком отзывов + блоком рейтинга/количества.
- REST endpoint `GET /api/reviews` (используется Vue‑виджетом).

## 🚀 Установка и настройка

### 1. Установка зависимостей

```bash
composer install
npm install
```

### 2. Настройка окружения

```bash
cp .env.example .env
php artisan key:generate
```

### 3. Настройка базы данных

В файле `.env`:
```env
DB_CONNECTION=sqlite
```

Создать файл базы данных:
```bash
touch database/database.sqlite
```

### 4. Настройка пользователя для входа

В файле `.env` добавьте (опционально):
```env
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=password
ADMIN_NAME=Admin
```

### 5. Миграции и сидеры

```bash
php artisan migrate --seed
```

### 6. Сборка фронтенда

```bash
npm run build
# или для разработки:
npm run dev
```

### 7. Запуск сервера

```bash
php artisan serve
```

Приложение будет доступно по адресу: `http://localhost:8000`

## 🔐 Авторизация

По умолчанию:
- **Email**: `admin@example.com` (или из `.env` `ADMIN_EMAIL`)
- **Пароль**: `password` (или из `.env` `ADMIN_PASSWORD`)

## 📖 Как пользоваться

1. **Войти:** `http://localhost:8000/login` + данные из `.env`.
2. **Настройка:** вкладка «Настройка» → вставьте ссылку вида `https://yandex.ru/maps/org/.../1234567890/` → «Сохранить».
3. **Отзывы:** вкладка «Отзывы» автоматически подгружает список (демо данных).

## 🔧 API

`GET /api/reviews`  
Возвращает JSON вида:

```json
{
  "success": true,
  "data": {
    "reviews": [
      { "id": 1, "author": "Иван", "rating": 5, "text": "..." }
    ],
    "rating": 4.7,
    "total_reviews": 148
  }
}
```

Сейчас данные статичны. После подключения реального провайдера они станут «живыми».

## 📁 Структура

```
app/
├── Http/
│   ├── Controllers/
│   │   ├── Auth/LoginController.php
│   │   └── Api/{SettingController,ReviewController}.php
│   └── Requests/Setting/StoreRequest.php
├── Models/{User,Setting}.php
├── Providers/AppServiceProvider.php       # биндинг интерфейсов
├── Repositories/Setting/*                 # слой доступа к БД
└── Services/
    ├── Settings/*                         # логика настроек
    └── Reviews/*                          # логика отзывов + провайдеры

resources/
├── js/
│   ├── app.js                             # регистрация Vue компонентов
│   ├── bootstrap.js                       # axios + CSRF
│   └── components/ReviewsComponent.vue
└── views/
    ├── layouts/app.blade.php
    ├── auth/login.blade.php
    ├── settings/index.blade.php
    └── reviews/index.blade.php
```

## 🔍 Как включить реальное API

1. Получить ключ/endpoint Яндекс.Карт.
2. В `app/Services/Reviews/ReviewsService.php` раскомментировать:
   ```php
   return $this->provider->getReviews($organizationId, $limit);
   ```
3. При необходимости настроить заголовки/токены в `YandexReviewsProvider`.
4. Vue начнёт отображать реальные данные.

## ⚙️ Конфигурация

### Переменные окружения

```env
# База данных
DB_CONNECTION=sqlite

# Учетные данные администратора
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=password
ADMIN_NAME=Admin
```

## 🧪 Тесты

Базовые тесты пока не написаны.  
Рекомендуемые сценарии:
- успешный логин;
- сохранение настроек;
- формат ответа `/api/reviews`.

## 🐛 Решение проблем

### Ошибка «Не удалось извлечь ID»
- проверьте, что ссылка заканчивается на `/1234567890/`.

### Ошибка «Не удалось получить отзывы»
- в демо‑режиме данные уходят всегда; если подключён реальный API — смотрите `storage/logs/laravel.log`.

## 📄 Лицензия

MIT License
